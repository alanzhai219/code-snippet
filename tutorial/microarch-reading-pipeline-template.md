mindmap
  root((Intel AI/向量计算指令))
    FMA
      数据类型
        FP32
        FP64
      宽度
        标量
          32-bit 标量
          64-bit 标量
        向量
          128-bit (XMM)
          256-bit (YMM)
          512-bit (ZMM, 需 AVX-512 FMA)
      结果类型
        与输入同精度
      典型场景
        通用浮点乘加
        HPC/科学计算
    VNNI
      数据类型
        INT8 点积
          int8 x int8 -> int32 累加
          signed/unsigned 组合变体
        INT16 点积
          int16 x int16 -> int32 累加
      宽度
        AVX-512 VNNI
          128-bit (XMM, 配合 VL)
          256-bit (YMM, 配合 VL)
          512-bit (ZMM)
        AVX-VNNI
          128-bit (XMM)
          256-bit (YMM)
      结果类型
        主要是 int32 累加
      典型场景
        INT8 推理
        量化 GEMM/Conv
    AMX
      计算模型
        Tile(二维块) 计算
        不是传统 128/256/512 向量宽度
      数据类型(按扩展)
        AMX-INT8
          int8 x int8 -> int32 累加
        AMX-BF16
          bf16 x bf16 -> fp32 累加
        AMX-FP16
          fp16 x fp16 -> fp32 累加
      宽度/粒度
        tile 寄存器
          8 个 tmm 寄存器
          单 tile 最大 16 行 x 64 字节
        K 维有效元素数
          INT8: 每行最多 64 元素
          BF16/FP16: 每行最多 32 元素
      典型场景
        大块矩阵乘
        高吞吐 AI 推理/训练# 微架构阅读与排流水一页笔记模板

> 适用场景：阅读 CPU 微架构/性能分析文章后，快速形成可执行的优化结论。
>
> 分析框架：前端 / 后端 / 端口 / 依赖 / 访存。

---

## 0. 基本信息

- 文章标题：
- 原文链接：
- CPU 平台与代际：
- 测试对象（算子/内核/循环）：
- 输入规模：
- 目标指标（吞吐/延迟/功耗）：

---

## 1. 三句话结论（先写）

1. 当前主瓶颈：前端 / 后端核心 / 访存 / 推测失误（圈选）
2. 最受限资源：
3. 优先优化动作（前 2 项）：

---

## 2. 前端（Frontend）

### 2.1 关键信息摘录

- 取指/译码宽度（每周期）：
- uop cache/LSD 相关描述：
- 分支预测行为与代价：
- 是否存在复杂指令或微码路径：

### 2.2 现象与判断

- 是否出现喂不饱后端：是 / 否
- 热点循环分支是否难预测：是 / 否
- 前端是否为一级瓶颈：是 / 否

### 2.3 可执行动作

- 降低不可预测分支密度
- 控制代码体积，提升 I-cache/uop-cache 命中
- 避免高代价复杂指令路径

---

## 3. 后端（Backend Core）

### 3.1 关键信息摘录

- 发射宽度（issue/dispatch）：
- 提交宽度（retire）：
- ROB/RS/LB/SB 等队列容量：
- 执行单元配置（ALU/FMA/Load/Store/Branch）：

### 3.2 现象与判断

- 最容易满的结构：
- 是否出现执行资源闲置但整体变慢：是 / 否
- 后端核心是否为一级瓶颈：是 / 否

### 3.3 可执行动作

- 减少无效 uop
- 调整指令构成，贴合执行单元能力
- 降低结构性冲突（队列压力）

---

## 4. 端口（Port Pressure）

### 4.1 关键信息摘录

- 高频指令及端口映射：
- 最拥挤端口：
- AGU/Load/Store 竞争情况：

### 4.2 现象与判断

- 是否存在单端口过载：是 / 否
- 端口冲突是否主导性能：是 / 否

### 4.3 可执行动作

- 改用端口更均衡的等价指令
- 打散同类指令，减少集中抢占
- 控制 shuffle/blend 等高压指令比例

---

## 5. 依赖（Dependency / Critical Path）

### 5.1 关键信息摘录

- 关键数据依赖链：
- 循环携带依赖（loop-carried dependency）：
- 关键操作 latency 与 throughput：

### 5.2 现象与判断

- 是否被长依赖链限制：是 / 否
- 是否存在 load-use 距离过短：是 / 否
- 依赖是否为一级瓶颈：是 / 否

### 5.3 可执行动作

- 循环展开 + 多路累加器
- 提前发起 load，拉开 load-use 距离
- 软件流水化（把下一轮准备前移）

---

## 6. 访存（Memory）

### 6.1 关键信息摘录

- 访问模式（连续/跨步/随机）：
- L1/L2/L3 命中行为：
- TLB 行为与页表相关开销：
- 带宽上限与当前利用情况：

### 6.2 现象与判断

- 是否 memory-bound：是 / 否
- 主要损耗来源：cache miss / TLB miss / 带宽 / 伪共享
- 访存是否为一级瓶颈：是 / 否

### 6.3 可执行动作

- 数据布局优化（连续、对齐、分块）
- 提高数据复用，减少访存次数
- 预取友好化（访问规律化）

---

## 7. 五维评分卡（1-5 分）

| 维度 | 分数（1-5） | 证据一句话 | 优先级 |
|---|---:|---|---:|
| 前端 |  |  |  |
| 后端 |  |  |  |
| 端口 |  |  |  |
| 依赖 |  |  |  |
| 访存 |  |  |  |

> 说明：分数越高，越可能是当前主瓶颈。

---

## 8. 优化计划（只保留前三项）

1. 动作：
   - 目标瓶颈：
   - 预期收益：
   - 风险与回退：

2. 动作：
   - 目标瓶颈：
   - 预期收益：
   - 风险与回退：

3. 动作：
   - 目标瓶颈：
   - 预期收益：
   - 风险与回退：

---

## 9. 验证闭环

- 基线版本：
- 优化版本：
- 指标对比（吞吐/延迟/命中/端口压力）：
- 结论是否验证假设：是 / 否
- 若不一致，下一轮假设：

---

## 10. 复盘（100 字内）

- 本文最有迁移价值的点：
- 对当前项目可立即落地的动作：
- 仍需补齐的知识：

---

## 附：快速检查清单（排流水）

- 是否存在长依赖链且未被并行工作掩盖
- load 是否发得足够早，load-use 是否合理
- FMA/ALU/Load/Store/Shuffle 端口是否均衡
- 分支是否可预测，是否可改为分支消除
- 展开后寄存器压力是否导致溢出
- 尾处理是否破坏主循环节奏
